# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
name = "ziplyn"
version = "1.0.0"
authors = ["Vaibhavee Singh <singhvaibhavee1@gmail.com>"]
build = false
exclude = [
    "tests/*",
    "examples/*",
    "docs/*",
    "*.data",
    "target/*",
    "Cargo.toml.orig",
    "*.tmp",
    "*.log",
    "*.bak",
    "*.orig",
    "*.largefile",
    "test_data",
    "*.pdf",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A fast and lightweight file compression and extraction tool built in Rust."
readme = false
license = "MIT"
repository = "https://github.com/Vaibhavee89/ziplyn"
documentation = "https://docs.rs/ziplyn"
keywords = ["compression", "extraction", "zip"]
categories = ["command-line-utilities","compression"]

[[bin]]
name = "ziplyn"
path = "src/main.rs"

[dependencies.flate2]
version = "1.0"

[dependencies.zip]
version = "0.6"
